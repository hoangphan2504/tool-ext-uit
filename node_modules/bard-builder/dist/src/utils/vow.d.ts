export declare class Lock {
    private promise;
    private resolve;
    constructor();
    wait(): Promise<any>;
    release(): void;
}
declare class Vow {
    handle<T>(promise: Promise<T>): Promise<Error | T>;
    lock(): Lock;
}
declare const vow: Vow;
export default vow;
