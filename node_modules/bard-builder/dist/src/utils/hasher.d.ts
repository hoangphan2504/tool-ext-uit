declare class Hasher {
    private static readonly CHARACTER_NORMAL;
    private static readonly CHARACTER_SAFE;
    constructor();
    create(length: number, safe?: boolean): string | false;
    extract(data: string): string | false;
    private generate;
}
declare const hasher: Hasher;
export default hasher;
