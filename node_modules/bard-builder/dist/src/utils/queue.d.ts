export type ComparationFunction<T> = (value: T) => boolean;
export default class Queue<T> {
    private head;
    private tail;
    private size;
    constructor();
    push(value: T): (Error | null);
    pull(): (T | Error);
    find(operator: ComparationFunction<T>): (T | Error);
    items(): IterableIterator<T>;
}
