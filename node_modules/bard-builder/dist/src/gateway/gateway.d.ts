import Message from "./message";
export default class Gateway {
    private incoming_queue;
    private outgoing_queue;
    constructor();
    pushIncoming(message: Message): Error | null;
    pullIncoming(): Error | Message;
    pushOutgoing(message: Message): Error | null;
    pullOutgoing(): Error | Message;
}
