import { Chain } from "./definition";
import Node from "./node";
export declare enum FlowTypes {
    INCOMING = "incoming",
    TRAILING = "trailing",
    OUTGOING = "outgoing"
}
export default class Flow {
    private nodes;
    constructor();
    private setup;
    getNode(name: string, type?: FlowTypes): (Error | Node);
    getNodes(type?: FlowTypes): (Error | Map<string, Node>);
    insertNode(name: string, chain: Chain, type?: FlowTypes): (Error | null);
}
